{
  "$id": "https://slides.local/schemas/slides.render_parity_verify.input.json",
  "type": "object",
  "required": ["request_id", "tool_id", "context", "inputs", "params"],
  "properties": {
    "request_id": { "type": "string" },
    "tool_id": { "const": "slides.render_parity_verify" },
    "context": { "$ref": "../common.json#/$defs/ActionContext" },
    "inputs": {
      "type": "object",
      "required": ["deck", "pptx"],
      "properties": {
        "deck": { "$ref": "../common.json#/$defs/DeckRef" },
        "pptx": { "$ref": "../common.json#/$defs/ArtifactRef" }
      },
      "additionalProperties": false
    },
    "params": { "type": "object", "additionalProperties": false }
  },
  "additionalProperties": false
}
