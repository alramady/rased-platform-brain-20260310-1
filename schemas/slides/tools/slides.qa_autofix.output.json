{
  "$id": "https://slides.local/schemas/slides.qa_autofix.output.json",
  "type": "object",
  "required": ["request_id", "tool_id", "status", "refs"],
  "properties": {
    "request_id": { "type": "string" },
    "tool_id": { "const": "slides.qa_autofix" },
    "status": { "type": "string", "enum": ["ok", "failed"] },
    "refs": {
      "type": "object",
      "required": ["deck"],
      "properties": { "deck": { "$ref": "../common.json#/$defs/DeckRef" } },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
